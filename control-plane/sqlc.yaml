version: "2"
sql:
  - engine: "sqlite"
    queries: "db/queries/queries.sql"
    schema: "db/schema.sql"
    gen:
      go:
        package: "dbgen"
        out: "db/dbgen"
        emit_json_tags: true
        emit_empty_slices: true
