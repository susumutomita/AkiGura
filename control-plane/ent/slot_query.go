// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"srv.exe.dev/ent/facility"
	"srv.exe.dev/ent/ground"
	"srv.exe.dev/ent/municipality"
	"srv.exe.dev/ent/notification"
	"srv.exe.dev/ent/predicate"
	"srv.exe.dev/ent/slot"
)

// SlotQuery is the builder for querying Slot entities.
type SlotQuery struct {
	config
	ctx               *QueryContext
	order             []slot.OrderOption
	inters            []Interceptor
	predicates        []predicate.Slot
	withFacility      *FacilityQuery
	withMunicipality  *MunicipalityQuery
	withGround        *GroundQuery
	withNotifications *NotificationQuery
	withFKs           bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SlotQuery builder.
func (_q *SlotQuery) Where(ps ...predicate.Slot) *SlotQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SlotQuery) Limit(limit int) *SlotQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SlotQuery) Offset(offset int) *SlotQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SlotQuery) Unique(unique bool) *SlotQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SlotQuery) Order(o ...slot.OrderOption) *SlotQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryFacility chains the current query on the "facility" edge.
func (_q *SlotQuery) QueryFacility() *FacilityQuery {
	query := (&FacilityClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(slot.Table, slot.FieldID, selector),
			sqlgraph.To(facility.Table, facility.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, slot.FacilityTable, slot.FacilityColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMunicipality chains the current query on the "municipality" edge.
func (_q *SlotQuery) QueryMunicipality() *MunicipalityQuery {
	query := (&MunicipalityClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(slot.Table, slot.FieldID, selector),
			sqlgraph.To(municipality.Table, municipality.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, slot.MunicipalityTable, slot.MunicipalityColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGround chains the current query on the "ground" edge.
func (_q *SlotQuery) QueryGround() *GroundQuery {
	query := (&GroundClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(slot.Table, slot.FieldID, selector),
			sqlgraph.To(ground.Table, ground.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, slot.GroundTable, slot.GroundColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNotifications chains the current query on the "notifications" edge.
func (_q *SlotQuery) QueryNotifications() *NotificationQuery {
	query := (&NotificationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(slot.Table, slot.FieldID, selector),
			sqlgraph.To(notification.Table, notification.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, slot.NotificationsTable, slot.NotificationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Slot entity from the query.
// Returns a *NotFoundError when no Slot was found.
func (_q *SlotQuery) First(ctx context.Context) (*Slot, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{slot.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SlotQuery) FirstX(ctx context.Context) *Slot {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Slot ID from the query.
// Returns a *NotFoundError when no Slot ID was found.
func (_q *SlotQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{slot.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SlotQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Slot entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Slot entity is found.
// Returns a *NotFoundError when no Slot entities are found.
func (_q *SlotQuery) Only(ctx context.Context) (*Slot, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{slot.Label}
	default:
		return nil, &NotSingularError{slot.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SlotQuery) OnlyX(ctx context.Context) *Slot {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Slot ID in the query.
// Returns a *NotSingularError when more than one Slot ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SlotQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{slot.Label}
	default:
		err = &NotSingularError{slot.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SlotQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Slots.
func (_q *SlotQuery) All(ctx context.Context) ([]*Slot, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Slot, *SlotQuery]()
	return withInterceptors[[]*Slot](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SlotQuery) AllX(ctx context.Context) []*Slot {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Slot IDs.
func (_q *SlotQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(slot.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SlotQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SlotQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SlotQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SlotQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SlotQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SlotQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SlotQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SlotQuery) Clone() *SlotQuery {
	if _q == nil {
		return nil
	}
	return &SlotQuery{
		config:            _q.config,
		ctx:               _q.ctx.Clone(),
		order:             append([]slot.OrderOption{}, _q.order...),
		inters:            append([]Interceptor{}, _q.inters...),
		predicates:        append([]predicate.Slot{}, _q.predicates...),
		withFacility:      _q.withFacility.Clone(),
		withMunicipality:  _q.withMunicipality.Clone(),
		withGround:        _q.withGround.Clone(),
		withNotifications: _q.withNotifications.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithFacility tells the query-builder to eager-load the nodes that are connected to
// the "facility" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SlotQuery) WithFacility(opts ...func(*FacilityQuery)) *SlotQuery {
	query := (&FacilityClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFacility = query
	return _q
}

// WithMunicipality tells the query-builder to eager-load the nodes that are connected to
// the "municipality" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SlotQuery) WithMunicipality(opts ...func(*MunicipalityQuery)) *SlotQuery {
	query := (&MunicipalityClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMunicipality = query
	return _q
}

// WithGround tells the query-builder to eager-load the nodes that are connected to
// the "ground" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SlotQuery) WithGround(opts ...func(*GroundQuery)) *SlotQuery {
	query := (&GroundClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGround = query
	return _q
}

// WithNotifications tells the query-builder to eager-load the nodes that are connected to
// the "notifications" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SlotQuery) WithNotifications(opts ...func(*NotificationQuery)) *SlotQuery {
	query := (&NotificationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNotifications = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SlotDate time.Time `json:"slot_date,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Slot.Query().
//		GroupBy(slot.FieldSlotDate).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SlotQuery) GroupBy(field string, fields ...string) *SlotGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SlotGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = slot.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SlotDate time.Time `json:"slot_date,omitempty"`
//	}
//
//	client.Slot.Query().
//		Select(slot.FieldSlotDate).
//		Scan(ctx, &v)
func (_q *SlotQuery) Select(fields ...string) *SlotSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SlotSelect{SlotQuery: _q}
	sbuild.label = slot.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SlotSelect configured with the given aggregations.
func (_q *SlotQuery) Aggregate(fns ...AggregateFunc) *SlotSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SlotQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !slot.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SlotQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Slot, error) {
	var (
		nodes       = []*Slot{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withFacility != nil,
			_q.withMunicipality != nil,
			_q.withGround != nil,
			_q.withNotifications != nil,
		}
	)
	if _q.withFacility != nil || _q.withMunicipality != nil || _q.withGround != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, slot.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Slot).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Slot{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withFacility; query != nil {
		if err := _q.loadFacility(ctx, query, nodes, nil,
			func(n *Slot, e *Facility) { n.Edges.Facility = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMunicipality; query != nil {
		if err := _q.loadMunicipality(ctx, query, nodes, nil,
			func(n *Slot, e *Municipality) { n.Edges.Municipality = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withGround; query != nil {
		if err := _q.loadGround(ctx, query, nodes, nil,
			func(n *Slot, e *Ground) { n.Edges.Ground = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withNotifications; query != nil {
		if err := _q.loadNotifications(ctx, query, nodes,
			func(n *Slot) { n.Edges.Notifications = []*Notification{} },
			func(n *Slot, e *Notification) { n.Edges.Notifications = append(n.Edges.Notifications, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SlotQuery) loadFacility(ctx context.Context, query *FacilityQuery, nodes []*Slot, init func(*Slot), assign func(*Slot, *Facility)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Slot)
	for i := range nodes {
		if nodes[i].facility_slots == nil {
			continue
		}
		fk := *nodes[i].facility_slots
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(facility.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "facility_slots" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SlotQuery) loadMunicipality(ctx context.Context, query *MunicipalityQuery, nodes []*Slot, init func(*Slot), assign func(*Slot, *Municipality)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Slot)
	for i := range nodes {
		if nodes[i].municipality_slots == nil {
			continue
		}
		fk := *nodes[i].municipality_slots
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(municipality.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "municipality_slots" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SlotQuery) loadGround(ctx context.Context, query *GroundQuery, nodes []*Slot, init func(*Slot), assign func(*Slot, *Ground)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Slot)
	for i := range nodes {
		if nodes[i].ground_slots == nil {
			continue
		}
		fk := *nodes[i].ground_slots
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(ground.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "ground_slots" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SlotQuery) loadNotifications(ctx context.Context, query *NotificationQuery, nodes []*Slot, init func(*Slot), assign func(*Slot, *Notification)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Slot)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Notification(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(slot.NotificationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.slot_notifications
		if fk == nil {
			return fmt.Errorf(`foreign-key "slot_notifications" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "slot_notifications" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *SlotQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SlotQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(slot.Table, slot.Columns, sqlgraph.NewFieldSpec(slot.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, slot.FieldID)
		for i := range fields {
			if fields[i] != slot.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SlotQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(slot.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = slot.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SlotGroupBy is the group-by builder for Slot entities.
type SlotGroupBy struct {
	selector
	build *SlotQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SlotGroupBy) Aggregate(fns ...AggregateFunc) *SlotGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SlotGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SlotQuery, *SlotGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SlotGroupBy) sqlScan(ctx context.Context, root *SlotQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SlotSelect is the builder for selecting fields of Slot entities.
type SlotSelect struct {
	*SlotQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SlotSelect) Aggregate(fns ...AggregateFunc) *SlotSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SlotSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SlotQuery, *SlotSelect](ctx, _s.SlotQuery, _s, _s.inters, v)
}

func (_s *SlotSelect) sqlScan(ctx context.Context, root *SlotQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
