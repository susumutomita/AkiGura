// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"srv.exe.dev/ent/municipality"
)

// Municipality is the model entity for the Municipality schema.
type Municipality struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// ScraperType holds the value of the "scraper_type" field.
	ScraperType string `json:"scraper_type,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// Enabled holds the value of the "enabled" field.
	Enabled bool `json:"enabled,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MunicipalityQuery when eager-loading is set.
	Edges        MunicipalityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MunicipalityEdges holds the relations/edges for other nodes in the graph.
type MunicipalityEdges struct {
	// Grounds holds the value of the grounds edge.
	Grounds []*Ground `json:"grounds,omitempty"`
	// Slots holds the value of the slots edge.
	Slots []*Slot `json:"slots,omitempty"`
	// ScrapeJobs holds the value of the scrape_jobs edge.
	ScrapeJobs []*ScrapeJob `json:"scrape_jobs,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// GroundsOrErr returns the Grounds value or an error if the edge
// was not loaded in eager-loading.
func (e MunicipalityEdges) GroundsOrErr() ([]*Ground, error) {
	if e.loadedTypes[0] {
		return e.Grounds, nil
	}
	return nil, &NotLoadedError{edge: "grounds"}
}

// SlotsOrErr returns the Slots value or an error if the edge
// was not loaded in eager-loading.
func (e MunicipalityEdges) SlotsOrErr() ([]*Slot, error) {
	if e.loadedTypes[1] {
		return e.Slots, nil
	}
	return nil, &NotLoadedError{edge: "slots"}
}

// ScrapeJobsOrErr returns the ScrapeJobs value or an error if the edge
// was not loaded in eager-loading.
func (e MunicipalityEdges) ScrapeJobsOrErr() ([]*ScrapeJob, error) {
	if e.loadedTypes[2] {
		return e.ScrapeJobs, nil
	}
	return nil, &NotLoadedError{edge: "scrape_jobs"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Municipality) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case municipality.FieldEnabled:
			values[i] = new(sql.NullBool)
		case municipality.FieldID, municipality.FieldName, municipality.FieldScraperType, municipality.FieldURL:
			values[i] = new(sql.NullString)
		case municipality.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Municipality fields.
func (_m *Municipality) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case municipality.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case municipality.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case municipality.FieldScraperType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field scraper_type", values[i])
			} else if value.Valid {
				_m.ScraperType = value.String
			}
		case municipality.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				_m.URL = value.String
			}
		case municipality.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				_m.Enabled = value.Bool
			}
		case municipality.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Municipality.
// This includes values selected through modifiers, order, etc.
func (_m *Municipality) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGrounds queries the "grounds" edge of the Municipality entity.
func (_m *Municipality) QueryGrounds() *GroundQuery {
	return NewMunicipalityClient(_m.config).QueryGrounds(_m)
}

// QuerySlots queries the "slots" edge of the Municipality entity.
func (_m *Municipality) QuerySlots() *SlotQuery {
	return NewMunicipalityClient(_m.config).QuerySlots(_m)
}

// QueryScrapeJobs queries the "scrape_jobs" edge of the Municipality entity.
func (_m *Municipality) QueryScrapeJobs() *ScrapeJobQuery {
	return NewMunicipalityClient(_m.config).QueryScrapeJobs(_m)
}

// Update returns a builder for updating this Municipality.
// Note that you need to call Municipality.Unwrap() before calling this method if this Municipality
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Municipality) Update() *MunicipalityUpdateOne {
	return NewMunicipalityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Municipality entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Municipality) Unwrap() *Municipality {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Municipality is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Municipality) String() string {
	var builder strings.Builder
	builder.WriteString("Municipality(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("scraper_type=")
	builder.WriteString(_m.ScraperType)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(_m.URL)
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.Enabled))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Municipalities is a parsable slice of Municipality.
type Municipalities []*Municipality
